# ═══════════════════════════════════════════════════════════════════════════
# SICOT — endurecimiento para producción (override sobre docker-compose.yml)
#
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
#
# Qué cambia respecto al archivo base (pensado para desarrollo en una sola
# máquina):
#   - Postgres deja de publicar su puerto al host: solo el backend, que está
#     en la misma red interna de Docker, puede hablarle por el nombre de
#     servicio "db". Nadie en la LAN/Internet puede conectarse directo a la
#     base de datos saltándose el backend y su capa de JWT.
#   - Adminer (panel visual de la BD, sin autenticación propia) ya no arranca
#     por defecto — queda detrás del profile "tools". Para usarlo puntualmente
#     en el servidor (con acceso restringido, p. ej. por túnel SSH), agregar
#     `--profile tools` al comando de arriba.
#
# Recordatorio importante (no lo resuelve este archivo): VITE_API_URL y
# CORS_ALLOWED_ORIGINS deben apuntar a la IP/dominio real del servidor, no a
# "localhost" — ver .env.example. Si se cambia VITE_API_URL hay que
# reconstruir el frontend (queda horneado en el build de Vite).
#
# Secretos y perfil — las tres garantías que añade este archivo:
#
#   1. DB_PASSWORD pasa de tener un valor de conveniencia por defecto (base,
#      "sicot_dev_password") a ser obligatorio: si el .env no lo define, este
#      override hace que `docker compose ... config`/`up` se niegue a arrancar
#      con un mensaje claro, en vez de arrancar tranquilamente con la
#      contraseña de desarrollo expuesta.
#   2. JWT_SECRET recibe el mismo trato obligatorio. El archivo base no le
#      define ningún valor por defecto, así que en desarrollo el respaldo lo
#      pone el perfil "dev" de Spring; aquí, además, se exige explícitamente
#      para que un despliegue no pueda quedarse sin secreto propio.
# TLS (ADR-009): este override añade un proxy inverso (Caddy) que pasa a ser el
# ÚNICO servicio con puertos publicados. El frontend y el backend dejan de
# publicarlos y solo se alcanzan por la red interna de Docker. Antes de esto, el
# JWT y las credenciales viajaban por la red del centro en texto claro.
#
#   3. SPRING_PROFILES_ACTIVE se fija a "prod" de forma literal, NO
#      interpolada desde el .env. Es deliberado: hace imposible desplegar el
#      perfil "dev" usando este archivo. Con "dev" activo se sembrarían las
#      cuentas de prueba (cuyas contraseñas están publicadas en el README) y
#      Swagger quedaría público.
# ═══════════════════════════════════════════════════════════════════════════

services:
  db:
    # "!reset" (no un simple "[]") porque Compose combina listas entre
    # archivos por defecto — un "ports: []" normal se fusiona con la lista del
    # archivo base y NO la vacía. "!reset" es la sintaxis de Compose que sí
    # borra lo heredado.
    ports: !reset []
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Defina DB_PASSWORD en el .env antes de usar el perfil de producción — no hay contraseña de conveniencia aquí}

  backend:
    environment:
      # Literal, no interpolado: el perfil "dev" no puede activarse por accidente
      # desde el .env cuando se despliega con este archivo.
      SPRING_PROFILES_ACTIVE: prod
      DB_PASSWORD: ${DB_PASSWORD:?Defina DB_PASSWORD en el .env antes de usar el perfil de producción — no hay contraseña de conveniencia aquí}
      JWT_SECRET: ${JWT_SECRET:?Defina JWT_SECRET en el .env antes de usar el perfil de producción — genérelo con `openssl rand -base64 48`, nunca reutilice el de desarrollo}
      # Administrador inicial: obligatorio en el PRIMER arranque de una base
      # vacía. Sin él, el despliegue quedaría inaccesible — DataInitializer solo
      # siembra cuentas en los perfiles dev/test, POST /api/usuarios exige rol
      # ADMINISTRADOR y el login es el único endpoint público. El backend lo
      # comprueba y se niega a arrancar (ver AdministradorInicial); esto lo
      # detiene antes, en `docker compose up`, con un mensaje más claro.
      # Una vez creada la cuenta, ambas variables pueden retirarse del .env.
      SICOT_ADMIN_EMAIL: ${SICOT_ADMIN_EMAIL:?Defina SICOT_ADMIN_EMAIL en el .env — es la cuenta de administrador con la que se entra por primera vez}
      SICOT_ADMIN_PASSWORD: ${SICOT_ADMIN_PASSWORD:?Defina SICOT_ADMIN_PASSWORD en el .env con al menos 12 caracteres — genérela con `openssl rand -base64 24` y cámbiela desde la aplicación tras el primer ingreso}
    # Deja de publicar el 8080 al host: el único camino hacia la API pasa por el
    # proxy con TLS. Con el puerto publicado seguiría existiendo un acceso sin
    # cifrar a los mismos datos, que es exactamente lo que ADR-009 prohíbe.
    ports: !reset []

  frontend:
    # Deja de publicar al host: ahora se llega por el proxy. Publicar los dos
    # caminos dejaría uno sin cifrar hacia los mismos datos, y ese nadie lo
    # vigila (ADR-009).
    ports: !reset []
    build:
      args:
        # Mismo origen: el proxy sirve la SPA y enruta /api al backend, así que
        # el navegador no necesita saber en qué host vive la API. Es además lo
        # que elimina el problema que advertía el README — que VITE_API_URL
        # queda horneado en el build y apuntando a "localhost" desde otra
        # máquina no resuelve.
        VITE_API_URL: ""

  adminer:
    profiles: ["tools"]
    ports: !reset []

  # ── Proxy inverso con TLS (ADR-009) ──────────────────────────────────────
  # Caddy obtiene y renueva certificados solo. Con SICOT_DOMINIO apuntando a un
  # dominio público usa ACME; con un nombre interno emite un certificado local.
  proxy:
    image: caddy:2-alpine
    container_name: sicot-proxy
    restart: unless-stopped
    environment:
      SICOT_DOMINIO: ${SICOT_DOMINIO:?Defina SICOT_DOMINIO en el .env — dominio público para certificado ACME, o nombre interno/IP para uno local}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      # Los certificados y las claves de cuenta ACME viven en volúmenes: sin
      # esto, cada `docker compose up` volvería a pedir un certificado nuevo y
      # agotaría los límites de emisión del proveedor en pocos días.
      - sicot_caddy_data:/data
      - sicot_caddy_config:/config
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    healthcheck:
      # La API de administración de Caddy, no el sitio: pedir el sitio por HTTP
      # devolvería la redirección a HTTPS y habría que decidir si eso cuenta
      # como sano. Esto pregunta lo que de verdad importa aquí — si el proxy
      # está en pie y con su configuración cargada.
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:2019/config/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  sicot_caddy_data:
  sicot_caddy_config:
