services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: sicot
      POSTGRES_USER: sicot
      POSTGRES_PASSWORD: ${DB_PASSWORD:-sicot_dev_password}
    volumes:
      - sicot_db_data:/var/lib/postgresql/data
    ports:
      # 5433 en el host para no chocar con un PostgreSQL nativo ya corriendo
      # en 5432 (como el que usa el flujo de desarrollo sin Docker).
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sicot -d sicot"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./sicot-backend
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://db:5432/sicot
      DB_USERNAME: sicot
      DB_PASSWORD: ${DB_PASSWORD:-sicot_dev_password}
      JWT_SECRET: ${JWT_SECRET:-ZGl2ZWxvcG1lbnQtc2ljb3Qtc2VjcmV0by1qd3Qta2V5LW11eS1sYXJnbzY0LWJ5dGVzLXNlZ3Vybw==}
      CORS_ALLOWED_ORIGINS: http://localhost:8443,http://127.0.0.1:8443
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  frontend:
    build:
      context: ./Sicot Frontend 1.0
      args:
        # El navegador del usuario llama a esta URL directamente, por eso debe
        # ser la URL alcanzable desde el host (no el nombre del servicio interno).
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8080}
    depends_on:
      - backend
    ports:
      - "8443:8443"

volumes:
  sicot_db_data:
